# AI Provider Compatibility Check

Use this template for one disposable visible-browser check against a
user-selected Chat Completions-compatible provider. Never record the API key,
Authorization header, raw bookmark payload, or full model response.

## Environment

- Date:
- Browser and version:
- Endpoint origin only:
- Provider/product:
- Model:
- Privacy mode: `domain-only` / `metadata`
- Scope size:

## Result

- Host permission requested after preview: yes / no
- Exact payload preview inspected: yes / no
- HTTP outcome (status only):
- Approximate latency:
- Structured schema accepted: yes / no
- Evidence validation accepted: yes / no
- Cancellation checked: yes / no
- Result remained read-only: yes / no
- Pass / fail:
- Sanitized notes:

If the provider fails, record only the sanitized error category and status.
Compatibility remains unknown until this check is completed with user-owned
credentials.
